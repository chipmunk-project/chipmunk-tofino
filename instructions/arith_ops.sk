generator int arith_op(int operand1, int operand2) {
  int opcode = ??(1);
  if (opcode == 0) {
    return operand1 + operand2;
  } else {
    assert(opcode == 1);
    return operand1 - operand2;
  }
}
